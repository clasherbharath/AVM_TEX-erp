<?php
/**
 * Application configuration.
 *
 * Adjust APP_BASE if the project is hosted in a different folder.
 */
declare(strict_types=1);

define('APP_BASE', '/avm-tex');

/** Set true to show exact SQL/PDO errors on forms (disable in production). */
define('APP_DEBUG', true);
